# 验证记录

日期：2026-09-25。环境：Windows x64，192 DPI（200%），.NET SDK 10.0.401 / Runtime 10.0.12。

## 2026-09-25 钱包补充

- 普通 Hone key 的现场查询：subscription 为无限 key 限额，usage 折算约 $4.76；同一 key 调用账户 `/api/user/self` 返回 401。已登录网页显示钱包约 $26.82、账户总消费约 $283.18。这是当时的读数，会随用量变化。
- 新增独立 Hone 钱包数据源；63 项检查通过，新增账户钱包 GET 链路、金额精度、CNY 站点汇率、缺字段 / 业务错误、401 / 403、未连接状态、卡片余额优先和配置隔离检查。
- 最终自包含 EXE：`dist/QuotaPeek.exe`，76,628,584 bytes，SHA-256 `8A23E755019A3735A757FDAF89D67352D4FCAF64016C9C869AD3A1FDD22F13BA`。已替换并启动正式目录版本；原 EXE 和设置备份在 `.artifacts/backups/before-wallet-20260925-185223/`。
- 用隔离演示实例的真实 Windows 设置窗口验证了点击“连接钱包余额”、独立令牌提示、保存、保留原数据源、重启恢复和表单回到顶部。余额卡片渲染图在 `.artifacts/wallet-desktop-20260925-184823/wallet-final-render.png`，其中金额明确是演示数据。
- 用户明确授权后，在 Hone 网页生成独立账户访问令牌；网站的密码身份验证由用户本人完成。令牌通过受保护输入框保存到 Windows 凭据管理器，设置文件只存数据源配置。
- 正式版“测试连接”成功显示钱包余额 $26.82。保存后程序重新从凭据管理器取令牌并同步成功：`Remaining=26.816294`、`Used=283.183706`、`Currency=USD`、`Status=Ok`。网页同时显示 $26.82 / $283.18；每 5 分钟自动刷新。原 Hone API 和 Codex 配置与凭据保留。
- 19:06 再次启动正式 `dist/QuotaPeek.exe`，钱包在启动后重新获取成功，`FetchedAt=2026-09-25T11:06:19.260465+00:00`；不是沿用测试输入框或旧缓存。已查看运行实例的真实卡片渲染：`.artifacts/wallet-live-20260925-190611/wallet-live.png`，显示钱包 $26.82、账户累计消费 $283.18、key 消费 $4.76。

以下是钱包改动前首版的验收记录。

## 交付物

- `D:\app\QuotaPeek\dist\QuotaPeek.exe`
- 大小：76,626,969 bytes（约 73.1 MiB），自包含、单文件、全部运行库解压加载。
- SHA-256：`C2FD6BA5F01407FB51453E299996557CAAC5A9769E15D025FA9CDB9DBE9168AC`
- `build.ps1 -Publish` 成功；未安装或修改系统 .NET，使用本机已有的 SDK。

## 已通过

1. **43 项 Core / 持久化检查**：中转站美分换算、无限额度、业务错误、DeepSeek 币种选择、小数精度、Kimi 欠费 / 国际站、SiliconFlow 总余额、OpenRouter null 上限、自定义字段、预算估算、Codex 多额度桶与动态窗口、Unix 秒时间、失败保留旧数据、退避 / Retry-After、充值排除、SQLite、损坏配置备份、Windows 凭据保存 / 读取 / 删除、跨阈值去重。
2. **真实 Codex**：本机官方 app-server 的 `account/rateLimits/read` 成功；未创建模型请求或会话。实际账户只返回每周窗口，组件正确显示一个窗口，没有补造 5 小时额度。17:50 截图显示 86% 剩余、10/02 16:22（本地时区）重置；数值会继续变化。
3. **最终 EXE 的 15 项 UIA 检查**：启动，TOOLWINDOW / NOACTIVATE / TOPMOST，排除任务栏，Hone / Codex 卡片，真实 Codex 数据，收起 / 展开，非法刷新间隔拒绝，设置保存，非敏感配置落盘，手动预算账户，关闭设置仍常驻，锁定穿透样式，程序触发热键处理后的解锁，正常退出。
4. **4 项位置 / 生命周期检查**：离屏配置自动回到工作区、胶囊位置和收起状态重启后无漂移、两次正常退出（exit code 0）。仅模拟离屏保存位置，没有实际拔掉显示器。
5. **视觉检查**：查看最终 EXE 的 WPF 渲染图，以及窗口 / 设置页截图；修复了进度条只读属性绑定、图标字体继承、设置窗口遮挡、胶囊初次恢复的位置偏移。设置保存按钮固定在底部。
6. **常驻短时采样**：相同代码候选，完整解压版工作集约 165.7 MiB / 私有内存 96.7 MiB；6 秒采样 CPU 时间 0.0781 秒。此前压缩程序集直接加载版约 309.1 / 182.7 MiB，采用完整解压以减少常驻占用。数据来自短时演示实例，不代表全天运行基准。
7. **正式实例已启动并复核**：从 `dist\QuotaPeek.exe` 启动正常数据目录 `%APPDATA%\QuotaPeek`。17:54 读取 SQLite 和实际窗口，Hone 为 `Setup / 待连接`，Codex 为 `Ok`，周额度剩余 85%，重置时间 10/02 16:22。此次工作集约 209.6 MiB、私有内存 130.0 MiB；与演示实例不同，已经加载真实凭据查询 / Codex 适配器。开机自启未开启。

本次桌面测试的配置、凭据命名空间、SQLite 和图片均与正式账户隔离。临时凭据测试只使用 `test-only-key` 并已删除。测试清理仅处理本次创建的 PID；未结束用户的 Codex、VS Code 或其他进程。

## 明确未完成的现场验证

- **Hone 真实余额 / 消费**：没有用户 key。公开 `/api/status` 返回 USD、`quota_per_unit=500000`；两个计费路由无认证返回 401。解析和换算已做测试，金额需要在本机输入 key 后与控制台核对。其他内置 API 提供商也只有协议 / fixture 检查。
- **真实鼠标拖动、穿透点击、物理快捷键、不抢焦点**：环境前台是 `LockApp.exe`，物理输入不可用。最终脚本标记 `environment-blocked`。UIA 调用、样式位检查和程序触发 WM_HOTKEY 不能替代真实桌面输入验收。
- 全屏游戏 / 演示切换、真实多显示器 DPI 切换、锁屏 / 睡眠后的系统事件、Windows 通知展示和实际登录自启未做现场触发；相关逻辑已实现，开机自启保持关闭。

## 本地证据

- 最终真实数据 EXE 检查：`.artifacts/desktop-20260925-175048/result.json`
- 最终真实卡片渲染：`.artifacts/desktop-20260925-175048/render.png`
- 位置 / 重启检查：`.artifacts/lifecycle-20260925-175202/result.json`
- 数据源验证入口：`tools/probe-hone.ps1`、`tools/probe_codex.py`

这些 `.artifacts` 文件仅本机保留，不进入 Git。未来再次验收可运行 README 中的检查命令。
