<Window x:Class="QuotaPeek.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="QuotaPeek" Width="370" SizeToContent="Height" WindowStyle="None" AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize"
        Topmost="True" ShowInTaskbar="False" ShowActivated="False" UseLayoutRounding="True" FontFamily="Segoe UI, Microsoft YaHei UI" FontSize="12" Foreground="{StaticResource Text}">
  <Border x:Name="Outer" Margin="8" CornerRadius="20" Background="{StaticResource Canvas}" BorderBrush="{StaticResource Line}" BorderThickness="1"
          PreviewMouseLeftButtonDown="DragStart" PreviewMouseMove="DragMove" PreviewMouseLeftButtonUp="DragEnd" LostMouseCapture="DragLostCapture"
          PreviewMouseWheel="Capsule_MouseWheel" MouseLeave="Capsule_MouseLeave">
    <Border.Effect><DropShadowEffect Color="#000000" BlurRadius="12" ShadowDepth="2" Opacity="0.26"/></Border.Effect>
    <Border.ContextMenu><ContextMenu><MenuItem x:Name="TaskbarDockMenu" Header="嵌入左下角任务栏" IsCheckable="True" Click="TaskbarDock_Click"/><Separator/><MenuItem Header="设置" Click="Settings_Click"/><MenuItem Header="刷新" Click="Refresh_Click"/><MenuItem Header="锁定 / 解锁鼠标穿透" Click="Lock_Click"/><Separator/><MenuItem Header="退出 QuotaPeek" Click="Exit_Click"/></ContextMenu></Border.ContextMenu>
    <Grid>
      <Grid x:Name="Capsule" Visibility="Collapsed" Height="48" Margin="12,0,10,0" Background="Transparent" ToolTip="滚轮切换钱包 / 额度 · 单击展开 · 按住拖动">
        <Grid.ColumnDefinitions><ColumnDefinition Width="20"/><ColumnDefinition Width="*"/><ColumnDefinition Width="26"/></Grid.ColumnDefinitions>
        <Ellipse x:Name="CapsuleDot" Width="7" Height="7" Fill="{StaticResource Mint}"/>
        <Button x:Name="ExpandButton" Grid.Column="1" Background="Transparent" Padding="3,0" Click="Expand_Click" AutomationProperties.Name="展开额度卡片">
          <TextBlock x:Name="CapsuleText" FontWeight="SemiBold" Text="QuotaPeek" FontSize="13" TextTrimming="CharacterEllipsis"/>
        </Button>
        <TextBlock Grid.Column="2" FontFamily="Segoe MDL2 Assets" Text="&#xE700;" FontSize="13" Foreground="{StaticResource Muted}" VerticalAlignment="Center" HorizontalAlignment="Right"
                   ToolTip="拖动组件"/>
      </Grid>
      <StackPanel x:Name="Expanded" Margin="18,15,18,14">
        <Grid x:Name="DragHeader" Margin="0,0,0,14" Background="Transparent">
          <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
          <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <Border Background="{StaticResource Mint}" CornerRadius="9" Width="30" Height="30" Margin="0,0,9,0"><TextBlock Text="Q" Foreground="#15231D" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
            <StackPanel><TextBlock Text="QuotaPeek" FontSize="15" FontWeight="SemiBold"/><TextBlock x:Name="Subtitle" Text="额度，抬眼可见" FontSize="10" Foreground="{StaticResource Muted}" Margin="0,2,0,0"/></StackPanel>
          </StackPanel>
          <Button x:Name="LockButton" Grid.Column="1" Style="{StaticResource IconButton}" Content="&#xE72E;" ToolTip="锁定鼠标穿透" AutomationProperties.Name="锁定鼠标穿透" Click="Lock_Click"/>
          <Button x:Name="SettingsButton" Grid.Column="2" Style="{StaticResource IconButton}" Content="&#xE713;" ToolTip="设置" AutomationProperties.Name="打开设置" Click="Settings_Click"/>
          <Button x:Name="CollapseButton" Grid.Column="3" Style="{StaticResource IconButton}" Content="&#xE70E;" ToolTip="收起" AutomationProperties.Name="收起额度卡片" Click="Collapse_Click"/>
        </Grid>
        <ScrollViewer x:Name="CardsScroll" MaxHeight="560" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Focusable="False">
          <ItemsControl x:Name="Cards">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Border Background="{StaticResource Panel}" CornerRadius="13" Padding="14,13" Margin="0,0,0,9">
                  <StackPanel>
                    <Grid Margin="0,0,0,10"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                      <StackPanel Orientation="Horizontal"><Border Width="27" Height="27" CornerRadius="8" Background="#283643" Margin="0,0,8,0"><TextBlock Text="{Binding Initial}" Foreground="{Binding Accent}" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/></Border><StackPanel VerticalAlignment="Center"><TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"/><TextBlock Text="{Binding Scope}" FontSize="9" Foreground="{StaticResource Muted}" Margin="0,2,0,0"/></StackPanel></StackPanel>
                      <TextBlock Grid.Column="1" Text="{Binding StatusText}" Foreground="{Binding StatusBrush}" FontSize="10" VerticalAlignment="Center"/>
                    </Grid>
                    <StackPanel Visibility="{Binding MoneyVisibility}">
                      <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding PrimaryValue}" FontSize="32" FontWeight="SemiBold" FontFamily="Segoe UI Variable Display, Segoe UI"/>
                        <TextBlock Grid.Column="1" Text="{Binding PrimaryLabel}" FontSize="10" Foreground="{StaticResource Muted}" VerticalAlignment="Bottom" Margin="6,0,0,6"/>
                      </Grid>
                      <TextBlock Text="{Binding SecondaryText}" FontSize="11" Foreground="{StaticResource Muted}" Margin="0,4,0,2"/>
                    </StackPanel>
                    <ItemsControl ItemsSource="{Binding Windows}"><ItemsControl.ItemTemplate><DataTemplate>
                      <StackPanel Margin="0,0,0,12">
                        <Grid Margin="0,0,0,7"><TextBlock Text="{Binding Name}" FontSize="11"/><TextBlock Text="{Binding Remaining}" Foreground="{Binding Accent}" HorizontalAlignment="Right" FontWeight="SemiBold" FontSize="12"/></Grid>
                        <ProgressBar Value="{Binding Percentage, Mode=OneWay}" Foreground="{Binding Accent}"/>
                        <TextBlock Text="{Binding Reset}" Foreground="{StaticResource Muted}" FontSize="10" Margin="0,5,0,0"/>
                      </StackPanel>
                    </DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                    <StackPanel Visibility="{Binding TrendVisibility}" Margin="0,7,0,5"><Viewbox Stretch="Fill" Height="34"><Polyline Width="286" Height="36" Points="{Binding Trend}" Stroke="{Binding Accent}" StrokeThickness="1.7" StrokeLineJoin="Round"/></Viewbox><TextBlock Text="{Binding TrendLabel}" FontSize="9" Foreground="{StaticResource Muted}" Margin="0,5,0,0"/></StackPanel>
                    <StackPanel Visibility="{Binding ForecastVisibility}" Margin="0,5,0,7"><TextBlock Text="{Binding Forecast}" Visibility="{Binding ForecastTextVisibility}" Foreground="{Binding Accent}" FontSize="10"/><TextBlock Text="{Binding ResetCredits}" Visibility="{Binding ResetsVisibility}" Foreground="{StaticResource Muted}" FontSize="10"/></StackPanel>
                    <TextBlock Text="{Binding Message}" Visibility="{Binding MessageVisibility}" TextWrapping="Wrap" FontSize="10" Foreground="{StaticResource Muted}" Margin="0,5,0,7"/>
                    <Button Content="连接账户" Visibility="{Binding SetupVisibility}" Background="#2D4453" Margin="0,0,0,8" Padding="8,7" HorizontalAlignment="Left" Click="Settings_Click" AutomationProperties.Name="连接账户"/>
                    <TextBlock Text="{Binding Updated}" FontSize="9" Foreground="{StaticResource Muted}" Margin="0,4,0,0"/>
                  </StackPanel>
                </Border>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>
        <TextBlock x:Name="EmptyText" Text="还没有启用的数据源，在设置中添加一个。" Foreground="{StaticResource Muted}" TextWrapping="Wrap" Visibility="Collapsed" Margin="0,10"/>
        <Grid Margin="2,2,0,0"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
          <StackPanel Orientation="Horizontal" VerticalAlignment="Center"><Ellipse Width="5" Height="5" Fill="{StaticResource Mint}" Margin="0,0,6,0"/><TextBlock x:Name="FooterText" Text="自动刷新已开启" FontSize="10" Foreground="{StaticResource Muted}"/></StackPanel>
          <Button x:Name="RefreshButton" Grid.Column="1" Style="{StaticResource IconButton}" Content="&#xE72C;" ToolTip="立即刷新" AutomationProperties.Name="立即刷新" Click="Refresh_Click"/>
        </Grid>
      </StackPanel>
    </Grid>
  </Border>
</Window>
